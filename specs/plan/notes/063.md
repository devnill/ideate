# Implementation Notes: 063

The curator is an opus-class agent (complex synthesis). Tools: Read, Write, Glob. It reads completed review output files + existing domain files + guiding-principles.md. It does not re-process prior cycles — each run is incremental (delta only).
