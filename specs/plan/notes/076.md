# Implementation Notes: 076

Source pattern: `skills/review/SKILL.md` lines 135-166 (Phase 3.5). Replicate in brrr Phase 6b (lines 362-460).

brrr currently uses `reviews/incremental/` (lines 86, 232) and `reviews/final/` (lines 358, 380, 403, 425, 446-457) while the review and execute skills use `archive/incremental/` and `archive/cycles/{N}/`. All `reviews/` paths in brrr must be normalized to `archive/` equivalents. This includes both the write paths (line 232, 358) and the read paths (line 86, completed items scan).
