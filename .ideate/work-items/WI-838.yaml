id: WI-838
type: work_item
title: Document AUTH_FAILURE error code in adapter contract and clean up doc references (close II1, M1)
complexity: small
scope:
  - path: mcp/artifact-server/docs/adapter-contract.md
    op: modify
  - path: docs/platform/adapter-interface.md
    op: modify
depends: []
blocks: []
criteria:
  - "mcp/artifact-server/docs/adapter-contract.md error code table (lines ~552-590) gains an AUTH_FAILURE entry: code, description (authentication failed after token rotation attempt), retryable:false, origin (remote adapter only)."
  - docs/platform/adapter-interface.md Section 7 gains the same entry and cross-references WI-833 where the code was introduced.
  - "M1 dead reference cleanup: any stale adapter-interface.md reference flagged by code-reviewer (a deleted section or renamed anchor) is removed or repointed."
  - Both documents cite D-211 or the relevant decision for the soft-deprecated metrics tools mention, if cited, remains consistent (no regression).
domain: adapter-equivalence
phase: PH-046
work_item_type: chore
notes: |-
  # WI-838: close cycle-9 II1 + M1

  ## II1 (gap-analyst Significant)

  WI-833 added the AUTH_FAILURE error code to the GraphQL client's error taxonomy (thrown when token rotation fails definitively). The error code is not documented in either of the two canonical adapter documentation surfaces:

  - mcp/artifact-server/docs/adapter-contract.md — the error code table (around lines 552-590) enumerates code, retryability, and origin for each StorageAdapterError subtype.
  - docs/platform/adapter-interface.md Section 7 — the platform-facing contract.

  P-85 (documentation surface enumeration) requires that error-code additions update both.

  ## M1 (code-reviewer Minor)

  Code-reviewer flagged a dead reference in adapter-interface.md (a section pointer that doesn't resolve after a prior rename). Check the spec-reviewer output for the exact line and either remove the reference or update the anchor.

  No code changes. Documentation only.

  Depends: optionally after WI-837 so the docs reflect the expanded AUTH_FAILURE trigger surface (tokenProvider throws + null return), but not strictly required.
resolution: "AUTH_FAILURE documented in adapter-contract.md (Section AUTH_FAILURE (StorageAdapterError)) and adapter-interface.md (Section 5.3). Rework addressed 4 significant review findings: fictional class name, wrong trigger condition, wrong code list, wrong section placement. All corrected and re-reviewed Pass."
status: done
cycle_created: 27
cycle_modified: 27
content_hash: 4e53441ae1c74b460980ac88ff7014b54fdb43e0a03d873ef5e4b88bd7748bae
token_count: 661
