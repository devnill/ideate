id: WI-740
type: work_item
title: Update architecture doc stale counts and renamed tool name (P-35, P-38)
status: done
complexity: low
scope: []
depends: []
blocks: []
criteria:
  - Architecture doc Section 5 lists `ideate_get_artifact_context` (not `ideate_get_work_item_context`)
  - Architecture doc shows '16 extension tables' (not 12)
  - Architecture doc shows '16 edge types' (not 10)
  - Architecture doc shows 'Tools (22)' (not 17)
  - Architecture doc shows 'Schema (v5)' (not v1)
  - No other sections of the architecture doc are modified
  - P-35 and P-38 violations are resolved
domain: artifact-structure
phase: null
notes: |-
  ## Context
  S1-spec (spec-reviewer) + S2-spec (spec-reviewer) from full-audit 2026-04-09. P-35 and P-38 violations in the architecture doc.

  ## Change
  This is an artifact-only work item. Do NOT edit any code files. Use MCP tools only.

  1. Read the current architecture artifact via `ideate_get_context_package()` — the architecture section is included.

  2. Make exactly 5 targeted text substitutions in the architecture content:
     - `ideate_get_work_item_context` → `ideate_get_artifact_context` (appears in the tools table, Section 5)
     - `12 extension tables` → `16 extension tables`
     - `10 edge types` → `16 edge types`
     - `Tools (17)` → `Tools (22)`
     - `Schema (v1)` → `Schema (v5)` (in the schema section header)

  3. Write back via `ideate_write_artifact({type: "architecture", id: "architecture", content: {content: <updated text>}})`.

  Do NOT change any other content. Do NOT rewrite or restructure sections. String replacement only.

  P-49: documentation changes only, no YAML backfill for artifacts.
resolution: "Applied 5 targeted substitutions to architecture doc: ideate_get_artifact_context, 16 extension tables (full 16-item list), 16 edge types, Tools (22), Schema (v5). Rework: added 4 missing table names (interview_questions, proxy_human_decisions, projects, phases) to parenthetical. P-35/P-38 resolved."
cycle_created: null
cycle_modified: 19
content_hash: ea06bff625336d36eb94985dc3040311007e0bd28badb4e041c4c74d22ad7bc9
token_count: 509
