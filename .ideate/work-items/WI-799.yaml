id: WI-799
type: work_item
title: Standardize PPR edge type weight keys to lower_snake_case; remove normalizeEdgeType
complexity: medium
scope:
  - path: mcp/artifact-server/src/ppr.ts
    op: modify
depends: []
blocks: []
criteria:
  - DEFAULT_EDGE_TYPE_WEIGHTS keys in ppr.ts are lower_snake_case (depends_on, governed_by, informed_by, references, blocks) — matching the canonical case used by schema.ts and the indexer.
  - normalizeEdgeType() function removed from ppr.ts.
  - All callers of normalizeEdgeType() within mcp/artifact-server/src/ updated to use raw edge type strings; caller enumeration done by grep during execution and recorded in the completion report.
  - "Existing PPR unit tests pass without modification (semantic equivalence: same weights apply to same edges)."
  - Equivalence test against RemoteAdapter (if present) still passes — D1 from RF-clean-interface-proposal is closed.
domain: agent-system
phase: PH-045
work_item_type: maintenance
notes: "P2 cleanup item from RF-clean-interface-proposal §3 (D1). The current code accepts mixed-case edge type keys (DEPENDS_ON, depends_on) by normalizing in normalizeEdgeType(). Server-side PPR uses lower_snake_case directly. Standardize on lower_snake_case to match schema.ts. Caller enumeration: grep for `normalizeEdgeType` across mcp/artifact-server/src/ during execution; update all call sites in the same WI (per user direction — not a separate spike). If callers extend beyond src/context/ppr.ts, surface in the completion report; the canonical scope here is the file containing the constants and the function. Depends on WI-798 to avoid touching CONTAINMENT_EDGE_TYPES while it still exists."
resolution: "Executed cycle 1 of PH-045. DEFAULT_EDGE_TYPE_WEIGHTS keys rewritten lower_snake_case, normalizeEdgeType removed along with weight-key and edge-type normalization loops. Full test suite (1388 tests) passes. Incremental review: Pass (one minor non-blocking note M1 re: EDGE_TYPE_REGISTRY coverage, not part of this WI's scope)."
status: done
cycle_created: 32
cycle_modified: 27
content_hash: 8a8cf6ea4bdf58e83c742d65b9e81c76264c78f0d8ee87050fc330fbf5d02e98
token_count: 540
