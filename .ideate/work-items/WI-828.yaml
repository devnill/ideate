id: WI-828
type: work_item
title: "Doc-sync follow-up: architecture-overview, ARCHITECTURE, adapter-method-specs"
complexity: small
scope:
  - path: docs/platform/architecture-overview.md
    op: modify
  - path: ARCHITECTURE.md
    op: modify
  - path: docs/platform/adapter-method-specs.md
    op: modify
depends: []
blocks: []
criteria:
  - architecture-overview.md:212 and :652 state archiveCycle return type as Promise<string> (not Promise<void>)
  - ARCHITECTURE.md:801 uses selectAdapter (not createAdapter) for the adapter factory
  - ARCHITECTURE.md:805 drops the createLocalAdapter factory claim and references the LocalAdapter class export directly
  - adapter-method-specs.md Section 17 describes strict canonical-only cycle_summary selection (targetRow = adherenceRow ?? summaryRow); no reference to da.cycle or file_path fallback
  - adapter-method-specs.md:827 divergence note removed or updated to reflect post-WI-824 behavior
domain: artifact-structure
phase: PH-045
work_item_type: maintenance
notes: |-
  # WI-828: Doc-sync follow-up

  Closes cycle-7 capstone findings:
  - code-reviewer S1 (architecture-overview.md archiveCycle return type)
  - gap-analyst S1 (adapter-method-specs.md Section 17 describes removed WI-824 behavior)
  - gap-analyst M1 (ARCHITECTURE.md stale factory references)

  These are all P-85 violations — documentation surfaces describing behavior changed by cycle 7 WIs that were not enumerated in their file scopes.

  ## Scope

  ### architecture-overview.md
  - Line 212: change the archiveCycle mention to `archiveCycle(cycle: number): Promise<string>` (returns human-readable archival summary)
  - Line 652: same fix

  ### ARCHITECTURE.md
  - Line 801: `createAdapter` factory → `selectAdapter` factory (actual export in server.ts:65)
  - Line 805: drop the `createLocalAdapter` factory claim. adapters/local/index.ts exports the LocalAdapter class directly with no factory function.

  ### adapter-method-specs.md
  - Section 17 (getConvergenceData) line 811: replace 'Looks in document_artifacts table for content. Falls back to reading YAML file from file_path.' with a strict canonical-only description matching reader.ts:1083–1101.
  - Line 827: remove 'da.cycle = ? OR file_path pattern %/cycles/NNN/%' language; replace with 'file_path ends with /spec-adherence.yaml or /summary.yaml'.

  ## Non-goals
  - No code changes. Documentation sync only.
  - Do not add unrelated sections.

  ## Approach
  1. Read adapter.ts:586 to confirm actual archiveCycle signature.
  2. Read server.ts:65 and adapters/local/index.ts to confirm selectAdapter + LocalAdapter class export shape.
  3. Read reader.ts:1083–1101 (canonical-only selection) for Section 17 rewrite material.
  4. Apply targeted edits.
resolution: "Closed cycle-7 capstone findings (code-reviewer S1, gap-analyst S1/M1). Doc surfaces synced: architecture-overview.md archiveCycle return type, ARCHITECTURE.md selectAdapter/LocalAdapter factory claims, adapter-method-specs.md Section 17 strict canonical-only. Incremental review: Pass, 0 findings."
status: done
cycle_created: 8
cycle_modified: 27
content_hash: 12d3828122d6a9fb0ca7c874e5ccaff33d09344b34d2303854b714dc3d96eef0
token_count: 802
