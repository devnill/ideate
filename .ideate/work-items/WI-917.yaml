id: WI-917
type: work_item
title: Enable tsconfig strict flags (noUnusedLocals, noUnusedParameters) and resolve violations
complexity: medium
scope:
  - path: mcp/artifact-server/tsconfig.json
    op: edit
depends:
  - WI-904
blocks: []
criteria:
  - "tsconfig.json compilerOptions has `noUnusedLocals: true` and `noUnusedParameters: true`"
  - tsc --noEmit exits 0 with the flags enabled — any violations across mcp/artifact-server/src resolved by removing unused imports, renaming unused params to _name, or using the value
  - Full vitest suite passes
domain: null
phase: PH-057
work_item_type: maintenance
notes: |-
  Sequenced after WI-904 (which touches writer.ts extensively). If WI-904 fixes any unused-import issues in writer.ts, this WI picks up from a cleaner baseline.

  Check tsc output after flag enable; fix each reported file. Common fixes: remove import that's no longer used (common after WI-897/898/899 consolidation), rename `args` to `_args` for intentionally unused parameters.
resolution: tsconfig.json has noUnusedLocals + noUnusedParameters. 16 files cleaned up across src/ and __tests__/ (unused imports removed, dead variables removed, one exhaustive-check variable marked void). tsc --noEmit exits 0. 1894 tests pass.
status: done
cycle_created: 83
cycle_modified: 27
content_hash: e11b50060f96f78b325db5bc23dbda1398e1d1daf22992bd15f859d1b55403fa
token_count: 348
