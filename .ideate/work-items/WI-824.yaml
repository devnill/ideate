id: WI-824
type: work_item
title: Fix convergence-checker reading legacy archived artifacts in reused cycle slots
complexity: null
scope:
  - path: mcp/artifact-server/src/
    op: modify
depends: []
blocks: []
criteria:
  - "Reproducer: autopilot run where workspace cycle slot contains legacy ID-named files (SA-NNN, CQ-NNN, GA-NNN) with verdict:Fail no longer returns condition_b:false when current-cycle named artifacts (code-quality, spec-adherence, gap-analysis) all show Pass"
  - ideate_get_convergence_status disambiguates current-cycle artifacts from unrelated legacy archived artifacts in the same workspace cycle slot
  - Convergence parser behavior on cycles with no collision is unchanged (regression test covers happy path)
  - Decision recorded (commit message or decision artifact) for which fix option was chosen — (a) content date/source/session-id disambiguation; (b) autopilot maps internal cycle to next-available slot; (c) parser ignores legacy ID-named files and reads only canonical named files — and why
  - "Discovery context preserved: failure observed during autopilot run 2026-04-14 where cycles/002/SA-002.yaml (March 29 archive) collided with autopilot internal cycle 2"
domain: null
phase: PH-045
work_item_type: bug
notes: |-
  # WI-824: Fix convergence-checker reading legacy archived artifacts in reused cycle slots

  ## Symptom

  `ideate_get_convergence_status` returns `condition_b:false` when legacy artifacts in the workspace cycle directory contain `verdict: Fail`, even when the current autopilot cycle's reviewer artifacts all show `Pass`.

  ## Discovery

  Observed during autopilot run on 2026-04-14. The file `cycles/002/SA-002.yaml` — a March 29 archive — collided with autopilot's internal cycle 2 directory, causing the convergence parser to read the old fail verdict instead of (or in addition to) the current cycle's pass verdicts.

  ## Root cause

  Autopilot's internal cycle numbering (1, 2, 3, ...) reuses workspace cycle directory slots that may already contain unrelated archived artifacts from prior unrelated runs. The convergence checker reads all artifacts in the cycle slot rather than scoping to artifacts produced by the current run.

  ## Fix options (decide during refine)

  - **(a)** convergence-checker disambiguates by content date / source / session-id — preserves both old and new files in the same slot
  - **(b)** autopilot maps internal cycle numbers to the next-available workspace cycle slot, avoiding collision entirely
  - **(c)** convergence-checker reads only the named files (`code-quality`, `spec-adherence`, `gap-analysis`) and ignores legacy ID-named files (`SA-NNN`, `CQ-NNN`, `GA-NNN`)

  Option (c) is likely the cheapest and aligns with the post-WI-807 named-file convention; (b) avoids the collision class entirely but requires autopilot state changes; (a) is the most defensive but adds parser complexity.

  ## Reproducer

  Run autopilot in a workspace whose `cycles/NNN/` slots contain legacy archived files (e.g., `SA-NNN.yaml` with `verdict: Fail`) at slot numbers that autopilot's internal counter will reuse. Observe `condition_b: false` from `ideate_get_convergence_status` even when the current cycle's reviewer artifacts all show `Pass`.

  ## Scope

  Likely in the convergence parser inside `mcp/artifact-server/src/` (handler for `ideate_get_convergence_status`). Autopilot skill files may be touched if option (b) is selected.

  ## Not in scope

  - Migrating or deleting the legacy archived files themselves
  - Reworking autopilot's cycle numbering scheme beyond what option (b) would require

  ## Relationship to WI-817

  WI-817 is the original triage of this same bug (cycle 4). WI-824 was created via fresh triage on 2026-04-14 after the user reproduced and confirmed to proceed despite the duplicate flag. Resolve duplication during refine — likely close one as superseded.

  ## Notes

  Triage item — not assumed execution-ready. Run /ideate:refine to confirm fix option, scope, and module spec impact before /ideate:execute.
resolution: "Fixed convergence-checker strict canonical-only (option c). Removed dbContentRow fallback after first review caught reindex-leak (legacy SA-NNN after rebuildIndex populates da.cycle). Final: targetRow = adherenceRow ?? summaryRow, matches only spec-adherence.yaml / summary.yaml. 1421 tests pass. Added reindex-leak regression test at cycle 203. RemoteAdapter unchanged (no fallback chain). Rework x1."
status: done
cycle_created: null
cycle_modified: 27
content_hash: 488fe3557c39745c350bf10ef9b69e9092616a49d35cd66d0a1a4e44ed24a7ce
token_count: 1152
