id: WI-798
type: work_item
title: Remove CONTAINMENT_EDGE_TYPES dead code from ppr.ts
complexity: small
scope:
  - path: mcp/artifact-server/src/context/ppr.ts
    op: modify
depends: []
blocks: []
criteria:
  - CONTAINMENT_EDGE_TYPES constant declaration removed from ppr.ts.
  - All references to CONTAINMENT_EDGE_TYPES inside ppr.ts removed (the constant is unused — verify by grep before deletion).
  - "PPR adjacency-building behavior unchanged: existing PPR tests pass."
  - No new schema.ts edge types introduced; D2 (RF-clean-interface-proposal) is closed by deletion, not migration.
domain: agent-system
phase: PH-045
work_item_type: chore
notes: P2 cleanup item from RF-clean-interface-proposal §3 (D2). The constant exists in ppr.ts but corresponds to no edge type registered in schema.ts. It is dead code that misleads readers into thinking containment is filtered during adjacency building. Verify by grepping for `CONTAINMENT_EDGE_TYPES` and `containment` across mcp/artifact-server before deletion. If any caller exists outside ppr.ts, surface the finding before proceeding — declared scope in this WI is ppr.ts only.
resolution: "Closed via PHD-001-01 (cycle 1 Andon). Premise false: CONTAINMENT_EDGE_TYPES is actively used at mcp/artifact-server/src/ppr.ts:190 to skip containment edges during PPR adjacency construction. Not dead code. Separately, declared scope path src/context/ppr.ts does not exist (actual: src/ppr.ts). RF-clean-interface-proposal D2 recommendation was incorrect. See D-212."
status: obsolete
cycle_created: 32
cycle_modified: 27
content_hash: 049de744cd7926692ef294631164c255db98ea378f65f9601532490afebd3f53
token_count: 416
