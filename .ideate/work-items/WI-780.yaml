id: WI-780
type: work_item
title: Add integration test file for edge_type_weights validation
complexity: small
scope:
  - path: mcp/artifact-server/tests/adapters/edge-type-weights-validation.test.ts
    op: create
depends:
  - WI-779
blocks: []
criteria:
  - Integration test file exists at tests/adapters/edge-type-weights-validation.test.ts
  - Test exercises ValidatingAdapter wrapping a real LocalAdapter (not a mock)
  - Test verifies valid edge_type_weights accepted by LocalAdapter traverse
  - Test verifies invalid key throws INVALID_EDGE_TYPE before PPR runs
  - Test verifies negative value throws INVALID_EDGE_WEIGHT before PPR runs
  - Test verifies empty object is accepted
domain: artifact-structure
phase: PH-041
work_item_type: chore
notes: |-
  Follow the pattern established by tests/adapters/always-include-types-validation.test.ts and tests/adapters/seed-ids-validation.test.ts.

  Create a fresh temp directory with .ideate/ structure, open a real database, create a ValidatingAdapter wrapping a LocalWriterAdapter + LocalReaderAdapter, and exercise the edge_type_weights validation through the full adapter stack.

  Minimum test cases:
  1. Valid edge_type_weights with a real EdgeType key and numeric value — accepted, traverse completes
  2. Invalid key (not in ALL_EDGE_TYPES) — throws INVALID_EDGE_TYPE
  3. Negative value — throws INVALID_EDGE_WEIGHT
  4. Empty object {} — accepted

  Per P-82: adapter-integrated modules require integration test file in tests/adapters/.
resolution: Created tests/adapters/edge-type-weights-validation.test.ts with 11 integration tests exercising ValidatingAdapter wrapping real LocalAdapter. Covers valid weights, empty object, invalid key, negative/NaN/Infinity values, non-object/array rejection. All 1354 tests pass.
status: done
cycle_created: 25
cycle_modified: 25
content_hash: f0303329f1086bea95f8cf03b14837e45de55970f0826bfb7538f6689964e955
token_count: 483
