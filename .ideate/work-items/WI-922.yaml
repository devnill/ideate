id: WI-922
type: work_item
title: "Close cycle-76 missed Significants: context.ts N+1 + writer.ts isLinkedByDepends + server.ts void"
complexity: small
scope:
  - path: mcp/artifact-server/src/adapters/local/context.ts
    op: edit
  - path: mcp/artifact-server/src/adapters/local/writer.ts
    op: edit
  - path: mcp/artifact-server/src/server.ts
    op: edit
  - path: mcp/artifact-server/src/__tests__/adapters/writer.test.ts
    op: edit
depends: []
blocks: []
criteria:
  - context.ts traverse() no longer executes N+1 db.prepare().get(id) calls per PPR node. Replaced with a single WHERE id IN (?,?,...) batched query matching the pattern at alwaysTypeRows fetch three lines below.
  - writer.ts isLinkedByDepends BFS now seeds from both the in-batch dependsGraph AND the pre-existing SQLite edges of type depends_on for the relevant item IDs. Two work items linked via a previously-created WI in the DB are no longer falsely flagged as scope-collision.
  - server.ts:274,277 `void state.ctx.adapter.indexFiles(...)` and `void state.ctx.adapter.removeFiles(...)` replaced with `.catch(err => log.error(...))` so incremental index failures surface in logs.
  - "Regression test for isLinkedByDepends: create WI-A in cycle 1 (via putNode), then in cycle 2 call writeWorkItems([WI-B, WI-C]) with WI-B depends:[WI-A] and WI-C depends:[WI-A]. Assert the batch succeeds (WI-B and WI-C are linked via WI-A which is in the DB, not in-batch)."
  - Full vitest suite passes; tsc --noEmit passes.
domain: null
phase: PH-057
work_item_type: bug
notes: |-
  Closes cycle 76 code S1 + S3 + M3 in one coordinated fix. Three locations, all narrow:

  1. context.ts:199-205: pprNodeIds -> batch SELECT WHERE id IN (?,?,...). Use the existing sql`in` helper or inline drizzle.
  2. writer.ts:827-847: add a SELECT source_id, target_id FROM edges WHERE edge_type='depends_on' AND (source_id IN (?,...) OR target_id IN (?,...)) before the BFS; merge edges into dependsGraph.
  3. server.ts:274,277: `state.ctx.adapter.indexFiles(yamlChanged).catch(err => log.error('watcher', 'indexFiles failed', err));`
resolution: Three fixes landed. context.ts N+1 batched via WHERE id IN (?,?,...). writer.ts isLinkedByDepends seeds dependsGraph from existing SQLite depends_on edges (via SELECT before BFS) so cross-batch chains are visible. server.ts void async callbacks replaced with .catch(err => log.error(...)). 2 regression tests in writer.test.ts under WI-922 describe block (cross-batch dependency chain). 1896 tests pass (1894+2).
status: done
cycle_created: 84
cycle_modified: 27
content_hash: 9dc5b9ccc244d3581ebeed929cdbb0bf6ff0c34f6ca772f6b78065a33823a49d
token_count: 665
