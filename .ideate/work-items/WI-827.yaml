id: WI-827
type: work_item
title: Add getMetricsEvents to shared adapter contract test suite (II1, EC1)
complexity: null
scope:
  - path: mcp/artifact-server/src/adapters/
    op: modify
  - path: mcp/artifact-server/src/__tests__/
    op: modify
depends: []
blocks: []
criteria:
  - Shared contract test for getMetricsEvents runs against both LocalAdapter and RemoteAdapter and passes for both
  - Test covers filters documented in adapter-interface.md (cycle, agent_type, etc.) and verifies TypeScript-side cycle filtering behavior
  - Test suite fails fast if either adapter implementation diverges from the documented contract
domain: null
phase: PH-045
work_item_type: feature
notes: |-
  # WI-827: getMetricsEvents contract test

  Addresses cycle-6 gap-analysis findings II1 (integration-incomplete) and EC1 (edge-case-not-covered).

  ## Gap

  `getMetricsEvents` is part of the adapter contract but not covered by the shared contract test suite. RemoteAdapter implementation (two round-trips + TypeScript filtering) and LocalAdapter implementation diverge without a unifying test.

  ## Approach

  Mirror the existing shared contract test pattern for other adapter methods. Parameterize over both adapters. Cover documented filters and edge cases (empty result, cycle filter, agent_type filter).
resolution: Added getMetricsEvents contract tests (5 tests) to runAdapterContractTests. Covers empty result, cycle filter, agent_type filter, combined AND semantics, and MetricsEventRow shape. 3 minor findings deferred (M1 shape extension, M2 work_item/phase filters, M3 claim accuracy) — non-blocking.
status: done
cycle_created: 7
cycle_modified: 27
content_hash: 30fe2c8d6864a49aa3e9669c5f0b759d5596750638c669f855d5e40294abb646
token_count: 438
