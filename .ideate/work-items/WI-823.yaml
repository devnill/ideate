id: WI-823
type: work_item
title: Fix stale JSDoc in remote getMetricsEvents (S1)
complexity: low
scope:
  - path: mcp/artifact-server/src/adapters/remote/index.ts
    op: modify
depends: []
blocks: []
criteria:
  - Replace the line at remote/index.ts:1035 reading '* Fetch metrics events in a single round-trip.' with text accurately describing the two-round-trip pattern.
  - "New JSDoc text mentions: two round-trips, queryNodes + getNodes, TypeScript-side filtering (including cycle), O(n) scan."
  - No occurrence of 'single round-trip' remains anywhere in src/adapters/remote/index.ts.
  - Test suite passes (npm test in mcp/artifact-server).
domain: agent-system
phase: PH-045
work_item_type: bug
notes: |-
  # Cycle 6 — fix S1 from cycle 5 capstone

  WI-822 corrected the markdown documentation of getMetricsEvents in two places (adapter-contract.md and architecture-overview.md) but missed the JSDoc comment co-located with the source at `mcp/artifact-server/src/adapters/remote/index.ts:1035`.

  The stale comment reads:
  ```
  * Fetch metrics events in a single round-trip.
  ```

  This contradicts the corrected docs. Replace it with text matching the corrected adapter-contract.md description (line 331), e.g.:
  ```
  * Fetch metrics events via two round-trips (queryNodes + getNodes);
  * filters (including cycle) are applied in TypeScript. O(n) scan.
  ```

  This is a 1-line change. No test changes required, but verify `npm test` still passes.
resolution: "Fixed: JSDoc at remote/index.ts:1035 replaced with two-round-trip description matching adapter-contract.md line 331. Tests pass (1419/0). Incremental review (cr823): Pass, 0 findings."
status: done
cycle_created: 6
cycle_modified: 27
content_hash: 7aae9431cb89ee452bbba7d80f4f27546ad219ed26c5795cf2f2d2a40d1d82e6
token_count: 452
