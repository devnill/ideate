id: WI-781
type: work_item
title: Audit StorageAdapter abstraction boundary leaks
complexity: small
scope:
  - path: mcp/artifact-server/src/tools/context.ts
    op: read
  - path: mcp/artifact-server/src/tools/query.ts
    op: read
  - path: mcp/artifact-server/src/tools/write.ts
    op: read
  - path: mcp/artifact-server/src/tools/analysis.ts
    op: read
  - path: mcp/artifact-server/src/tools/execution.ts
    op: read
  - path: mcp/artifact-server/src/server.ts
    op: read
depends: []
blocks:
  - WI-784
criteria:
  - Every import of LocalAdapter, LocalContextAdapter, LocalReaderAdapter, or LocalWriterAdapter outside of adapter selection code (server.ts:selectAdapter) is cataloged
  - Every direct reference to local-only types, methods, or behavior in tool handlers is cataloged
  - "Each leak includes: file path, line number, what is imported/referenced, why it bypasses StorageAdapter, and severity (blocks remote adapter usage vs cosmetic)"
  - Findings written as structured research artifact via MCP
domain: adapter-equivalence
phase: PH-042
work_item_type: spike
notes: |-
  Scan all files under mcp/artifact-server/src/tools/ and src/server.ts for imports from adapters/local/ that bypass the StorageAdapter interface.

  Known leak: tools/context.ts imports LocalContextAdapter directly for non-PPR context queries (architecture doc, principles, constraints, active project/phase). This means those queries would fail with a remote adapter.

  Also check:
  - tools/query.ts, tools/write.ts, tools/analysis.ts, tools/execution.ts for any direct adapter imports
  - server.ts for anything beyond selectAdapter that assumes local
  - Any instanceof checks against concrete adapter types
  - Any cast-to-concrete-type patterns
resolution: null
status: null
cycle_created: null
cycle_modified: null
content_hash: 68dbe0d182805d42fa84a3f6de23b36e051f2b0f0b7544488e98dbb2c11cde9f
token_count: 454
