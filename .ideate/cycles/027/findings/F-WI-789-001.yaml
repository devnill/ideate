id: F-WI-789-001
type: finding
work_item: WI-789
reviewer: main-session-incremental
severity: minor
verdict: pass
title: WI-789 incremental review — max_nodes pure-slice semantic
summary: |-
  Static + dynamic review of WI-789 (max_nodes semantic alignment). All ACs satisfied. Build green; 1394 tests pass, 332 skipped (Docker-dependent), 0 failed.

  ## AC status
  - AC-1 ppr.ts no longer aborts on graph size — satisfied (abort block + maxNodes validation + unused log import removed; maxNodes kept in PPROptions for backward compat with updated JSDoc noting it is ignored internally)
  - AC-2 LocalAdapter applies max_nodes as result-count slice after PPR sort — satisfied (adapters/local/context.ts: `ranked = ranked.slice(0, maxNodes)` after score sort, null/0 skip guard matches remote impl)
  - AC-3 Remote comment updated — satisfied (adapters/remote/index.ts line 845: "Apply max_nodes as a result-count cap per adapter contract")
  - AC-4 adapter.ts TraversalOptions.max_nodes JSDoc rewritten — satisfied ("Applied as a slice after PPR scoring. If omitted or 0, all ranked nodes are returned. No effect on PPR computation itself.")
  - AC-5 Tests rewritten and full suite green — satisfied (ppr.test.ts CR-002 abort test + Test 8 boundary + validation + large-graph stress rewritten to new pass-through contract; new equivalence-traverse.test.ts suite asserts ranked_nodes.length ≤ N without asserting score ordering per RF D5)

  ## Scope observations
  Diff is clean and scoped: 6 files, +164/-135. No out-of-scope modifications. Worker left the `log` import removal as a natural cleanup when its sole consumer was deleted — acceptable hygiene within scope.

  ## No findings
  No critical, significant, or minor issues identified. LocalAdapter and RemoteAdapter now share the same max_nodes contract (pure result-count cap, no PPR-computation side effects).
ac_verdicts:
  - criterion: ppr.ts no longer aborts on graph size > maxNodes
    status: satisfied
  - criterion: LocalAdapter applies max_nodes as result-count slice post-sort
    status: satisfied
  - criterion: Remote comment updated to match adapter contract language
    status: satisfied
  - criterion: adapter.ts TraversalOptions.max_nodes JSDoc rewritten to describe slice semantic
    status: satisfied
  - criterion: Tests rewritten and full suite green
    status: satisfied
cycle: 27
cycle_modified: 27
content_hash: 057d041d5e268b7ac76e81d50fc8220822e84b7c0d4c52cc6fa789ee9154718c
token_count: 597
