id: J-071-393
type: journal_entry
phase: execute
date: 2026-04-14
cycle_created: 71
title: work-item-complete
content: >-
  Work item WI-789: max_nodes pure-slice semantic alignment


  Status: complete

  Verdict: Pass

  Finding: F-WI-789-001 (cycle 27)


  ## Summary

  Aligned LocalAdapter with RemoteAdapter's existing max_nodes contract (pure
  result-count cap applied after PPR scoring). Removed the graph-size abort in
  `ppr.ts` so oversized graphs no longer return empty results. `computePPR` now
  ignores `maxNodes` internally; `LocalAdapter.traverse` slices `ranked` after
  score-descending sort. RemoteAdapter comment updated to match contract
  language. `TraversalOptions.max_nodes` JSDoc rewritten to describe the slice
  semantic.


  ## Files touched

  - mcp/artifact-server/src/adapter.ts (JSDoc)

  - mcp/artifact-server/src/ppr.ts (abort logic + validation + unused log import
  removed)

  - mcp/artifact-server/src/adapters/local/context.ts (post-sort slice)

  - mcp/artifact-server/src/adapters/remote/index.ts (comment)

  - mcp/artifact-server/src/__tests__/ppr.test.ts (rewritten to new contract)

  - mcp/artifact-server/tests/adapters/equivalence-traverse.test.ts (new suite)


  ## Verification

  - tsc clean; 1394 tests pass (332 skipped Docker-dependent), 0 failed on
  pre-merge branch

  - Post-merge full suite: 1388 tests pass, 0 failed


  Merged as non-fast-forward commit. Branch ideate/789-max-nodes-slice deleted.
