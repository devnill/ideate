id: J-071-363
type: journal_entry
phase: execute
date: 2026-04-13
cycle_created: 71
title: rework
content: >-
  ## WI-768 Rework — ValidatingAdapter fixes


  Incremental review returned Verdict: Fail with 2 significant + 4 minor
  findings. All 6 fixed:


  - **S1**: Removed `journal_entry` and `metrics_event` from CYCLE_SCOPED_TYPES
  — now matches writer.ts authoritative set (6 types)

  - **S2**: Removed cycle enforcement from nextId — cycle requirements are
  type-specific and handled by the inner adapter

  - **M1**: Added null/typeof guard in patchNode before `in` operator on
  `input.properties`

  - **M2**: Changed getDomainState error code from `INVALID_NODE_ID` to
  `INVALID_DOMAIN`

  - **M3**: Added `Array.isArray(input.properties)` check in validatePutNode

  - **M4**: Added edge validation loop in batchMutate for `input.edges`


  Post-rework: tsc --noEmit passes, all 1217 tests pass. Re-review spawned.
