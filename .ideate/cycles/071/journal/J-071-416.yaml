id: J-071-416
type: journal_entry
phase: autopilot
date: 2026-04-14
cycle_created: 71
title: work-item-complete
content: >
  ## WI-826 complete — adapter-interface.md fixes (D5, D6)


  **Verdict**: Pass after one rework


  ### Changes

  - Line 59: autopilot_state added to NodeType union under "Session/state
  artifacts" comment (D5)

  - JSDoc lines 443-459: per-adapter archiveCycle behavior documented (D6)

  - Section 4.7 Precondition + Atomicity rows: per-adapter distinction (D6)

  - Section 5.2 error table archiveCycle row: LocalAdapter never throws;
  RemoteAdapter throws ConnectionError/StorageAdapterError on transport failures
  (D6)


  ### Rework cycle

  First review found Significant S1: original D6 fix said archiveCycle "never
  throws" — accurate for LocalAdapter but wrong for RemoteAdapter (client.mutate
  throws on transport/HTTP/GraphQL errors with no try/catch wrapping). Worker
  reworked to document per-adapter behavior honestly without changing
  RemoteAdapter code (uniform-interface enforcement is out of scope for a
  doc-sync work item).


  ### Review verification

  Re-review Pass. All four D6 passages accurately distinguish:

  - LocalAdapter: writer.ts lines 1358-1393 — try/catch wraps transaction, all
  error paths return strings starting "Error during cycle archival"

  - RemoteAdapter: remote/index.ts:1441-1452 — bare client.mutate, propagates
  throws

  - client.mutate: throws on network failure (401/5xx → ConnectionError) and
  4xx/parse/empty-response (StorageAdapterError)


  2 items remaining in cycle 7: WI-824 (convergence-checker), WI-827
  (getMetricsEvents contract test).
