id: J-071-390
type: journal_entry
phase: execute
date: 2026-04-13
cycle_created: 71
title: work-item-complete
content: >-
  WI-787 done — enforced token_budget for always_include_types in PPR context
  assembly (Option 1: budget-capped always-include).


  **Files changed**:

  - `src/adapter.ts` — TraversalResult gains optional `budget_exhausted` +
  `truncated_types`; TraversalOptions.token_budget JSDoc documents the interface
  invariant (seeds force-included, everything else budget-gated).

  - `src/adapters/local/context.ts` — always-include loop now budget-gates
  non-seed nodes (fast-path using row.token_count before disk read) and tracks
  truncated NodeTypes.

  - `src/adapters/remote/index.ts` — client-side budget enforcement tracks
  budget_exhausted and truncated_types.

  - `src/tools/context.ts` — handleAssembleContext JSON metadata surfaces the
  new fields when present.

  - `tests/adapters/always-include-types-budget.test.ts` (created) — 7
  regression tests including the 16+16+arch default-budget boundary case.

  - `src/__tests__/adapter-contract.test.ts` — AC-5 contract test added to
  Traversal describe block.

  - `tests/adapters/equivalence-traverse.test.ts` — new dual-adapter test for
  AC-3: budget_exhausted + truncated_types equivalence under tiny budget.


  **Review**: F-WI-787-001 (Fail, significant: S1 missing RemoteAdapter overflow
  test, M1 wasted disk I/O, M2 AC-1 test title). Rework addressed all three.
  F-WI-787-002 (Pass, rework verdict). AC-3 verified end-to-end against Docker
  test stack; the 14 other equivalence-traverse failures are pre-existing and
  unrelated.


  **Verification**: pnpm exec tsc --noEmit clean; pnpm test --run → 1395 passed
  / 329 skipped / 0 failed; docker-backed equivalence test for WI-787 passed
  while 14 pre-existing equivalence failures persisted unchanged.


  **Bug resolution**: User-reported 226,481-character ideate_assemble_context
  overflow (2026-04-13) is now fixed. With the 16 GPs + 16 constraints +
  architecture workspace and default budget, total_tokens stays within 60000
  (50000 default + 20% margin).


  Next: PH-043 capstone review, then transition to PH-044.
